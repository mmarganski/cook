export type Dictionary = {
    tabs: {
        products: string,
        recipes: string,
        search: string,
        api: string
    },
    messages: {
        recipeUrl: string,
        callToSelect: string,
        loading: string,
        noMatch: string,
        loadingFail: string,
        notFound: string
    }
}
